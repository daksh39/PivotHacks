# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# development
/dev

# production
# build/ IS committed, so teammates can load the extension without building.
# Ignoring it here made git track the old CSS but silently skip each new
# hashed CSS file, leaving popup.html pointing at a stylesheet not in the repo.

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env
.env
